{
    "name": "brettjephson.dev",
    "version": "0.0.1",
    "private": true,
    "engines": {
        "pnpm": "8.15.8",
        "node": "20.9.0"
    },
    "scripts": {
        "deploy": "wrangler deploy",
        "dev": "wrangler dev",
        "start": "wrangler dev",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "wrangler": "^3.107.3"
    }
}
